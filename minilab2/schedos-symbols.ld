/* Define the location of the 'cursorpos' symbol. */

PROVIDE(cursorpos = 0x198000);

/* Define the location of the write lock as the last 4 bytes of the shared data */

PROVIDE(write_lock = 0x19996);